name: opto_sync_flutter_background
description: Flutter Workmanager bridge for bounded opto-sync background cycles.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.1
  flutter: '>=3.32.0'

dependencies:
  flutter:
    sdk: flutter
  opto_sync_client:
    path: ../dart
  workmanager: ^0.9.0+3

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
