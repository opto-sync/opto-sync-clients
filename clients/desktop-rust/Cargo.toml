[package]
name = "opto-sync-desktop"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
description = "Restart-safe desktop background synchronization contracts for opto-sync"
license = "MIT"
publish = false

[dependencies]
rusqlite = { version = "0.40.1", features = ["bundled"] }

[lints.rust]
unsafe_code = "forbid"
