# Credential-free client SDK example and conformance profile.
OPTO_SYNC_REQUIRE_BROWSER=0
OPTO_SYNC_CHROMIUM_PATH=
OPTO_SYNC_NATIVE_LIBRARY=

# Service URLs, database credentials, reusable tokens, signing material, and
# provider credentials belong to the invoking application or disposable E2E
# harness. This SDK repository must not become their owner.
