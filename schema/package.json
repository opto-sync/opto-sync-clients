{
  "name": "@opto-sync/envelope-schema-check",
  "private": true,
  "version": "0.0.0",
  "description": "Evaluates the shared ingest-envelope, SDK API, telemetry, and formal streaming JSON Schema contracts",
  "type": "module",
  "scripts": {
    "test": "node check-corpus.mjs && node check-sdk-api-contract.mjs && node check-telemetry-corpus.mjs && node check-formal-stream-corpus.mjs && node check-consistency-corpus.mjs",
    "lint:ores": "if [ -f ../.ores-lint/lint.sh ]; then sh ../.ores-lint/lint.sh; else echo 'ores-lint not installed'; fi",
    "prepublishOnly": "npm run lint:ores"
  },
  "devDependencies": {
    "ajv": "^8.17.1"
  },
  "oresLint": {
    "note": "Declared, not installed. ores-lint resolves these from a global install - see .ores-lint/README.md",
    "requires": {
      "eslint": ">=9",
      "typescript-eslint": ">=8 (optional; without it .ts files are skipped)"
    },
    "install": "npm i -g eslint typescript-eslint"
  }
}
