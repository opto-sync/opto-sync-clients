{
  "name": "@opto-sync/envelope-schema-check",
  "private": true,
  "version": "0.0.0",
  "description": "Evaluates the shared ingest-envelope JSON Schema against the fixture corpus every language validator is tested against",
  "type": "module",
  "scripts": {
    "test": "node check-corpus.mjs"
  },
  "devDependencies": {
    "ajv": "^8.17.1"
  }
}
